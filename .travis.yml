language: java

dist: trusty

before_install:

before_script:
  - chmod +x gradlew
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

script:
  - ./gradlew build

after_success:
  - bash <(curl -s https://codecov.io/bash)
